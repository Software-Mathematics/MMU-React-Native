import { SafeAreaView, Text, View } from "react-native";
import CatHead from "../../component/CatHead";

export default function ChangeRequest(){
    return(
        <SafeAreaView>
            <CatHead title={'Medicine Change Req.'}/>
        </SafeAreaView>
    )
}