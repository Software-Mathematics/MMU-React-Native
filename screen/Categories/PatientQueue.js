import { SafeAreaView, Text, View } from "react-native";
import CatHead from "../../component/CatHead";

export default function PatientQueue(){
    return(
        <SafeAreaView>
            <CatHead title={'Patient Queue'}/>
        </SafeAreaView>
    )
}